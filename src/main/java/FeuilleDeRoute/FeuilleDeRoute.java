package FeuilleDeRoute;

import Modele.Tournee;

/**
 * @author H4401
 * Classe utilisée pour générer une feuille de route après le calcul d'une tournée
 */
public class FeuilleDeRoute {

    public static void sortirFeuilleDeRoute(String filePath, Tournee tournee) {
        getVector(filePath);
    }

    private static void getVector(String f) {

    }
}
